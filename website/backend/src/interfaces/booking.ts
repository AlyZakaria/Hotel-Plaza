interface booking {
    typenames: String,
    numOfEachType: String,
    checkin: String,
    checkout: String,
    userId: Number,
    totalAmount: Number,
    saleId: String
}

export default booking
